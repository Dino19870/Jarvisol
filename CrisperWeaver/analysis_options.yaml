# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

linter:
  # The lint rules applied to this project can be customized in the
  # section below to disable rules from the `package:flutter_lints/flutter.yaml`
  # included above or to enable additional rules. A list of all available lints
  # and their documentation is published at
  # https://dart-lang.github.io/linter/lints/index.html.
  #
  # Instead of disabling a lint rule for the entire project in the
  # section below, it can also be suppressed for a single line of code
  # or a specific dart file by using the `// ignore: name_of_lint` and
  # `// ignore_for_file: name_of_lint` syntax on the line or in the file
  # producing the lint.
  rules:
    # avoid_print: false  # Uncomment to disable the `avoid_print` rule
    # prefer_single_quotes: true  # Uncomment to enable the `prefer_single_quotes` rule
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    unnecessary_const: true
    unnecessary_new: true
    use_super_parameters: true

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated_plugin_registrant.dart"
    # Generated by `flutter gen-l10n` from lib/l10n/*.arb on every
    # `flutter pub get`; lint hits there are upstream issues, not ours.
    - "lib/l10n/generated/**"
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
    # Dossiers de backups et fichiers temporaires — exclus de l'analyse
    - "_backups/**"
    - "**/*_backup_*.dart"
    - "**/*_20260821*.dart"
    - "**/*_20260822*.dart"
    - "**/*_20260823*.dart"
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  # Promote the lint categories we care about most to errors so a
  # regression fails the build instead of silently piling up. We landed
  # at zero analyzer hits 2026-05-01 — keep it that way.
  errors:
    # Real defect classes — these are bugs, not style.
    use_build_context_synchronously: error
    avoid_print: error
    unused_local_variable: error
    unused_element: error
    unused_import: error
    # Type-inference failures often hide real surprises (List<dynamic>,
    # Map<dynamic, dynamic>, missing generic args). Treat as errors.
    inference_failure_on_function_invocation: error
    inference_failure_on_function_return_type: error
    inference_failure_on_instance_creation: error
    # Flutter-API deprecations should be fixed at the time the SDK
    # bumps, not allowed to rot for years (the prior 27 hits were
    # uncaught for several Flutter releases).
    deprecated_member_use: error