# crispasr.xcframework is a build artifact (regenerated by
# scripts/build_ios_xcframework.sh from the sibling CrispASR repo).
# The pbxproj has a path reference to it via wire_ios_xcframework.rb;
# fresh checkouts run the build script before `flutter build ios`.
crispasr.xcframework/
