build/
.dart_tool/
pubspec.lock
__pycache__/
*.pyc
*.o
*.a
*.so
*.dll
*.gguf
.bench-venv/
.fastembed_cache/
gguf-export
build-avx/
build-avx512/
build-blas/
build-llamafile/
.env
.claude/
target/
crispembed.local-backup/
# Local Claude Code session notes — agent continuity, not human docs.
# Project documentation lives in README.md, PLAN.md, LEARNINGS.md.
CLAUDE.md
handover-prompts/
hf-cache/
.claude/handover-*.md

# accidentally-committed CMake build trees (incl. VPS-path caches)
build-*/
build-embed-wasm/

# local model/blueprint downloads (this session)
.models-tmp/
.ds-blueprints/

# scratch/temp dir — was accidentally committed once (Tesseract tessdata,
# ~113 MB of pack bloat purged from history); never track it again
tmp/

# scan_cleanup benchmark output
scan-bench/

# wasm browser e2e
tests/wasm-browser/node_modules/
tests/wasm-browser/package-lock.json

# local build logs
webgpu-build.log

# Transcoda oracle dumper imports the AGPL reference package (clean-room: throwaway)
tools/dump_transcoda_reference.py
flutter/crispembed/macos/Libs/*.dylib

.codex/easyocr-scratch/

# Generated by scripts/vendor_rust_sources.sh for `cargo publish` only.
crispembed-sys/vendor/

# Staged into the Python package by python/setup.py at build time.
python/crispembed/POLICY.md
python/*.egg-info/

# c2pa prebuilt (fetched at configure time; see cmake/FindC2pa.cmake)
third_party/c2pa/
