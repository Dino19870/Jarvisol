{
  "_comment": [
    "Community-GGUF import matrix (A3). See PLAN.md 'Ecosystem-compat + input-parsing hardening'.",
    "",
    "WHY: we test our own cstr/* conversions, not the ecosystem's. Issue #33 was exactly that gap —",
    "the registry ships nomic-embed-text-v2-moe pointing at cstr/ GGUFs (which loaded), while the",
    "community GGUF users actually reach for first (nomic-ai/*-GGUF, a llama.cpp export) aborted with",
    "'missing required tensor attn.q.weight'. Nothing caught it before a user did.",
    "",
    "Each entry pins a THIRD-PARTY GGUF of a model we claim to support. The driver asserts it loads,",
    "reports the expected arch/dims, and produces a semantically sane embedding (the garbage guard —",
    "a model loaded with fabricated hparams still emits floats, so dim+rc alone prove nothing).",
    "",
    "Fields:",
    "  name          test id",
    "  repo/file     HuggingFace repo + filename of the COMMUNITY gguf",
    "  arch          expected general.architecture (documents which key family it exercises)",
    "  dim           expected embedding dimension — the silent-default trap (#33 would give 384)",
    "  n_layer       expected layer count (nomic: 12; the silent default is 6)",
    "  query_prefix / doc_prefix  task prefixes this model family requires",
    "  min_margin    cos(query, related) must exceed cos(query, unrelated) by at least this",
    "  ref_file      optional: our own conversion of the SAME model, for a cross-conversion A/B",
    "  min_ref_cos   optional: required cosine between the two conversions",
    "",
    "MEASURED (2026-07-16, q4_k GGUF vs HF fp32, tests/hf_parity_community.py): bge-small 0.9962 | MiniLM 0.9919 | nomic-v2-moe 0.9797 | nomic-v1.5 0.9515. min_hf_cos=0.95 is sized to sit under the q4_k floor while still catching a structural bug (wrong pooling/rope/dims), which craters cosine far below it.",
    "PRECISION CONTROL — the only way to tell 'quant floor' from 'our bug': re-run the SAME code path at f16/f32. bge-small f32 and nomic-v1.5 f16 both give per-stage cos=1.000000 vs HF at EVERY layer, proving both encoder paths (bert split-QKV/abs-pos AND nomic-bert fused-QKV/RoPE) are exact and every q4_k delta is quantization. Do this before ever calling a low cosine a bug.",
    "",
    "control_file / control_min_cos: a HIGHER-PRECISION GGUF (f16/f32) of the SAME model. tests/prove_quant_control.py runs it per-stage vs the HF fp32 reference; ~1.0 at every layer proves the encoder GRAPH is exact, so the shipped-quant gap (measured_hf_cos) is quantization, NOT our bug. This automates the manual f16/f32 control that separated 'quant floor' from 'our bug' for #33."
  ],
  "models": [
    {
      "name": "nomic-embed-text-v2-moe",
      "repo": "nomic-ai/nomic-embed-text-v2-moe-GGUF",
      "file": "nomic-embed-text-v2-moe.Q4_K_M.gguf",
      "arch": "nomic-bert-moe",
      "dim": 768,
      "n_layer": 12,
      "query_prefix": "search_query: ",
      "doc_prefix": "search_document: ",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "Regression guard for issue #33: llama.cpp export uses fused attn_qkv + ffn_*_exps tensors and nomic-bert-moe.* hparam keys. Before the fix this aborted at load; with hparams missing it would have run at 384-dim/6-layer.",
      "hf_repo": "nomic-ai/nomic-embed-text-v2-moe",
      "hf_trust_remote_code": true,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9797,
      "measured_note": "q4_k floor; f32/f16 control not run for this one (500MB+ HF model)",
      "control_file": "nomic-embed-text-v2-moe.f16.gguf",
      "control_min_cos": 0.999
    },
    {
      "name": "nomic-embed-text-v1.5",
      "repo": "nomic-ai/nomic-embed-text-v1.5-GGUF",
      "file": "nomic-embed-text-v1.5.Q4_K_M.gguf",
      "arch": "nomic-bert",
      "dim": 768,
      "n_layer": 12,
      "query_prefix": "search_query: ",
      "doc_prefix": "search_document: ",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "A THIRD arch string ('nomic-bert', distinct from nomic-bert-moe) — resolved by A2's arch-derived lookup with no new code. Guards a real latent bug: this GGUF declares nomic-bert.rope.freq_base=1000, but the pre-A2 loader read only bert.rope_theta and silently defaulted to 10000. Tensors resolve (fused attn_qkv), so it loaded, exited 0 and emitted a 768-dim vector — just a subtly wrong one. Effect is length-dependent: vs our cstr conversion (which also uses theta=1000), correct-vs-default cosine is a wash on a short query (0.9531 vs 0.9563, noise) but clearly better on long text (0.9435 vs 0.9278). That is exactly why rc+dim checks cannot catch it and the garbage guard/parity must.",
      "hf_repo": "nomic-ai/nomic-embed-text-v1.5",
      "hf_trust_remote_code": true,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9515,
      "measured_note": "LOWEST of the matrix, and it is NOT a bug: per-stage f16 vs HF fp32 is cos=1.000000 at ALL 12 layers (norms identical), so the nomic-bert graph (fused QKV + RoPE) is exact. The q4_k drop is real but purely quantization, concentrated in the LAST block (layer_10 0.9977 -> layer_11 0.9499 — a step, not smooth drift). This model's final layer is unusually quant-sensitive; prefer f16/q8 for retrieval quality.",
      "control_file": "nomic-embed-text-v1.5.f16.gguf",
      "control_min_cos": 0.999
    },
    {
      "name": "bge-small-en-v1.5",
      "repo": "CompendiumLabs/bge-small-en-v1.5-gguf",
      "file": "bge-small-en-v1.5-q4_k_m.gguf",
      "arch": "bert",
      "dim": 384,
      "n_layer": 12,
      "query_prefix": "Represent this sentence for searching relevant passages: ",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "Plain bert arch with SPLIT attn_q/k/v and absolute position embeddings — the opposite shape to the nomic entries, so the matrix covers both. Exercises pooling_type=2 (CLS), which maps to CrispEmbed pool_method=1; a mis-mapped pooling would still return a 384-dim vector, so only the garbage guard catches it.",
      "hf_repo": "BAAI/bge-small-en-v1.5",
      "hf_trust_remote_code": false,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9962,
      "measured_note": "per-stage f32 vs HF fp32 is cos=1.000000 at ALL 12 layers — the bert path (split QKV + absolute positions) is exact; all q4_k deltas are quantization",
      "control_file": "bge-small-en-v1.5-f32.gguf",
      "control_min_cos": 0.9999
    },
    {
      "name": "all-MiniLM-L6-v2",
      "repo": "second-state/All-MiniLM-L6-v2-Embedding-GGUF",
      "file": "all-MiniLM-L6-v2-Q4_K_M.gguf",
      "arch": "bert",
      "dim": 384,
      "n_layer": 6,
      "query_prefix": "",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "ref_file": "all-MiniLM-L6-v2-iq4_xs.gguf",
      "min_ref_cos": 0.9,
      "notes": "The one entry with a cross-conversion A/B: a community Q4_K_M vs our own cstr iq4_xs of the same model. min_ref_cos is deliberately loose (0.90) — the two differ in quant AND converter, so this gates 'same model' agreement, not bit-parity. NOTE its dim/n_layer (384/6) coincide with the loader's silent defaults, so for THIS entry the shape check proves little and the garbage guard carries the weight.",
      "hf_repo": "sentence-transformers/all-MiniLM-L6-v2",
      "hf_trust_remote_code": false,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9919,
      "measured_note": "q4_k floor; per-stage profile is smooth monotonic decay 0.9994 -> 0.9897, no step-change"
    },
    {
      "name": "gte-modernbert-base",
      "repo": "eranmazur/gte-modernbert-base-Q8_0-GGUF",
      "file": "gte-modernbert-base-q8_0.gguf",
      "arch": "modern-bert",
      "dim": 768,
      "n_layer": 22,
      "query_prefix": "",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "The FIRST modern-bert arch entry, and the one that proved community-BPE-encoder support is deeper than a #33 alias add. This llama.cpp export uses gpt2 byte-level BPE with merges in the tokenizer.ggml.merges KV ARRAY (not our tokenizer.merges tensor) and NO numeric tokenizer.ggml.type — only tokenizer.ggml.model='gpt2' + tokenizer.ggml.pre='modern-bert'. Before the fix the dispatch fell back to WordPiece (50368 vocab < the 100K SPM heuristic), tokenizing 'hello world' to the WRONG ids and emitting garbage (structural-gate emb_ln_out cos=0.58, cos(related) < cos(unrelated)). The fix: model-string-authoritative tokenizer dispatch, BPE merges from the KV array, the GPT-2 ByteLevel regex pre-tokenizer, plus loader aliases (attn_norm->ln1, ffn_norm->ln2, output_norm->final_norm, GeGLU-by-shape reroute of the fused ffn_up [H,2*inter]) and metadata (pre_ln, inverted dual RoPE theta: freq_base=global/160000, freq_base_swa=local/10000, sliding_window->local window, sliding_window_pattern->global_attn_every_n) and exact-erf GeGLU. Tokens now match HF exactly: [50281,25521,1533,50282].",
      "hf_repo": "Alibaba-NLP/gte-modernbert-base",
      "hf_trust_remote_code": true,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9996,
      "measured_note": "SHIPPED IS Q8_0 (not q4_k like the other entries). Final-embedding (CLS-pool + L2) cos vs HF sentence-transformers = 0.999602; per-stage q8_0 vs HF fp32 = 0.999928 at the emb_ln_out structural gate and 0.99986-0.99998 across all 22 layers. The residual is quantization: the f16 control (below) is cos=1.000000 at EVERY stage AND 0.999999 final, proving the tokenizer + modern-bert graph (GeGLU-erf, dual-theta RoPE, sliding-window attn) are exact.",
      "control_repo": "cstr/gte-modernbert-base-GGUF",
      "control_file": "gte-modernbert-base-f16.gguf",
      "control_min_cos": 0.9999
    },
    {
      "name": "granite-embedding-107m-multilingual",
      "repo": "lmstudio-community/granite-embedding-107m-multilingual-GGUF",
      "file": "granite-embedding-107m-multilingual-Q4_K_M.gguf",
      "arch": "bert",
      "dim": 384,
      "n_layer": 6,
      "query_prefix": "",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "The FIRST SentencePiece entry in the matrix — an XLM-RoBERTa-family embedder exported as arch 'bert' with tokenizer.ggml.model='t5' (250002-token unigram vocab + scores). Exercises the model-string->SPM dispatch (t5->type 2) added for the modern-bert fix, plus pooling_type=2 (CLS). NOTE it uses absolute position offset 0 (unlike the intfloat multilingual-e5 XLM-R models, which need offset 2 and whose community GGUFs omit bert.position_offset -> those do NOT parity-match; see PLAN).",
      "hf_repo": "ibm-granite/granite-embedding-107m-multilingual",
      "hf_trust_remote_code": false,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9961,
      "measured_note": "q4_k floor. Final-embedding (CLS-pool + L2) cos vs HF sentence-transformers = 0.996145; per-stage q4_k vs HF fp32 = 0.999951 at the emb_ln_out structural gate and 0.9928-0.9969 across all 6 layers. The f16 control (below) is cos=1.000000 at EVERY stage AND 1.000000 final, proving the SPM tokenizer + bert graph are exact and the q4_k gap is quantization.",
      "control_repo": "mrutkows/granite-embedding-107m-multilingual-GGUF",
      "control_file": "granite-embedding-107m-multilingual-f16.gguf",
      "control_min_cos": 0.999
    },
    {
      "name": "LFM2.5-Embedding-350M",
      "repo": "LiquidAI/LFM2.5-Embedding-350M-GGUF",
      "file": "LFM2.5-Embedding-350M-Q8_0.gguf",
      "arch": "lfm2",
      "dim": 1024,
      "n_layer": 16,
      "query_prefix": "",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "First DECODER-family (lfm2 hybrid ShortConv+attention) entry. Found by the autodownload matrix work: the official LiquidAI GGUF (a llama.cpp export) did NOT load — crispembed's lfm2_embed loader required our converter's lfm.* tensor names + lfm2.<our> hparam keys + a c/a layer_types string; the export uses blk.N.* tensors, canonical lfm2.* keys, no layer-types string, and a 2D [K,C] depthwise-conv weight (ours is [K,1,C]). Fixed with tensor aliases + hparam-key fallbacks + layer-type-from-tensor-presence + a memory-preserving conv-weight reshape to [K,1,C]. Same class as the modern-bert fix.",
      "hf_repo": "LiquidAI/LFM2.5-Embedding-350M",
      "hf_trust_remote_code": true,
      "min_hf_cos": 0.90,
      "measured_hf_cos": 0.99,
      "measured_note": "AUTHORITATIVE gate is the LFM2-specific per-stage harness (build/test-lfm2-diff vs the raw HF Lfm2BidirectionalModel, same CLS pooling), NOT sentence-transformers: q8_0 per-stage = 0.9999 at every stage incl post_embed (structural gate) and cls_norm (final pooled) on both a short AND a long text; the f16 control (LFM2.5-Embedding-350M-F16.gguf) = cos=1.000000 at EVERY stage (graph exact, q8 gap is quant). The looser 0.99 here is the sentence-transformers final-embedding cosine (mean 0.9896, per-text 0.977-1.0) — lower ONLY because ST applies its own pooling config, not the GGUF's CLS; hence min_hf_cos=0.90 for the ST-based guard. Garbage-guard margin 0.76. No control_file: the encoder f16/f32 control mechanism (prove_quant_control.py) is for the bert-family per-stage dumper; LFM2 uses test-lfm2-diff."
    },
    {
      "name": "Qwen3-Embedding-0.6B",
      "repo": "Qwen/Qwen3-Embedding-0.6B-GGUF",
      "file": "Qwen3-Embedding-0.6B-Q8_0.gguf",
      "arch": "qwen3",
      "dim": 1024,
      "n_layer": 28,
      "query_prefix": "",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "First qwen3 DECODER-embed entry (last-token pooling, causal, gpt2-BPE decoder path — distinct from modern-bert's ENCODER BPE and LFM2's hybrid). Loaded CLEANLY from the OFFICIAL Qwen GGUF with no loader change: decoder_embed.cpp already accepts both dec.N.* and llama.cpp blk.N.* names, and the gpt2-BPE KV-merges tokenizer path (151387 merges) is already handled. A clean matrix add (contrast e5 = under-specified export, LFM2 = needed a loader fix).",
      "hf_repo": "Qwen/Qwen3-Embedding-0.6B",
      "hf_trust_remote_code": false,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9997,
      "measured_note": "Final-embedding cosine vs HF sentence-transformers (last-token pooling, which MATCHES crispembed — unlike LFM2's CLS/ST mismatch): q8_0 mean=0.999727 (per-text 0.99962-0.99977), f16 mean=1.000000 (per-text 1.000000 on all 4 texts) — the graph is EXACT and the q8 gap is quantization. Garbage-guard margin 0.58. No control_file: the prove_quant_control.py per-stage dumper is encoder-only; the f16 final-cosine=1.0 is the decoder-path control (Qwen3-Embedding-0.6B-f16.gguf, same repo)."
    },
    {
      "name": "gte-base-en-v1.5",
      "repo": "cstr/gte-base-en-v1.5-GGUF",
      "file": "gte-base-en-v1.5-q8_0.gguf",
      "arch": "bert",
      "dim": 768,
      "n_layer": 12,
      "query_prefix": "",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "Covers the GTE-v1.5 `NewModel` path: BERT WordPiece + NTK-scaled RoPE (theta 1022611) + fused **tanh**-GeGLU + CLS pooling (pool_type=2). This is the ONLY matrix entry exercising the tanh-GeGLU branch — it guards that modern-bert's arch-gated geglu_erf did not disturb the validated GTE-v1.5 tanh path. NOTE: this is a cstr/* (our own) GGUF, NOT a third-party ecosystem export — llama.cpp has no NewModel support, so no community GGUF exists; the entry is arch COVERAGE, not the ecosystem-compat signal the other entries carry.",
      "hf_repo": "Alibaba-NLP/gte-base-en-v1.5",
      "hf_trust_remote_code": true,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.99,
      "measured_note": "Validated per-stage (tests/test_encoder_diff.py) q8_0 vs HF fp32: emb_ln_out (structural gate) cos=0.999927, layer_0 0.9983, layer_5 0.9981, encoder_out (layer_11) 0.9926 — all PASS; the tanh-GeGLU NewModel graph is correct and the residual is q8 quant (concentrated in the last block, as usual). measured_hf_cos here is the encoder_out proxy; the structural gate confirms tokenization + graph.",
      "control_repo": "cstr/gte-base-en-v1.5-GGUF",
      "control_file": "gte-base-en-v1.5.gguf",
      "control_min_cos": 0.999
    },
    {
      "name": "all-mpnet-base-v2",
      "repo": "cstr/all-mpnet-base-v2-GGUF",
      "file": "all-mpnet-base-v2-q8_0.gguf",
      "arch": "bert",
      "dim": 768,
      "n_layer": 12,
      "query_prefix": "",
      "doc_prefix": "",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "Covers the MPNet path: two-stream self-attention + T5-style relative-position-bias (detected within the `bert` arch by rel-attn-bias tensors) + mean pooling. CrispEmbed is UNIQUE here — llama.cpp has no MPNet support, so no community GGUF exists; this is a cstr/* (our own) GGUF and the entry is arch COVERAGE, not an ecosystem-compat signal. It is the ONLY regression guard on the MPNet rel-attn-bias graph.",
      "hf_repo": "sentence-transformers/all-mpnet-base-v2",
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.997,
      "measured_note": "Final-embedding cosine vs HF (AutoModel mean-pool + L2-norm): realistic sentences 0.9967–0.9983, short 2-word text 0.9873 (mean-pool amplifies per-token delta on very short input). PRECISION CONTROL RAN INFORMALLY: f32 == q8_0 (0.9875 vs 0.9873 min), so the small residual is STRUCTURAL (the unique two-stream/rel-attn-bias graph), NOT quantization — which is exactly why NO control_file is set: an f32 control must clear >=0.99 (dump_encoder_reference has no MPNet two-stream path to run a per-stage control anyway). Realistic-text parity is already 0.997+; the entry guards the MPNet graph against regression via min_hf_cos + garbage_guard.",
      "no_control_reason": "f32 == q8_0 (~0.987 structural residual on short text), so an f32 control cannot clear the >=0.99 floor; MPNet has no dump_encoder_reference per-stage path"
    },
    {
      "name": "embeddinggemma-300m-qat",
      "repo": "cstr/embeddinggemma-300m-GGUF",
      "file": "embeddinggemma-300m-qat-q8_0-dense.gguf",
      "arch": "gemma-embedding",
      "dim": 768,
      "n_layer": 24,
      "query_prefix": "task: search result | query: ",
      "doc_prefix": "title: none | text: ",
      "query": "how do I bake sourdough bread",
      "related": "A step-by-step guide to baking sourdough loaves at home.",
      "unrelated": "The 1998 amendment to the municipal zoning ordinance.",
      "min_margin": 0.05,
      "notes": "Regression guard for the community/official 'gemma-embedding' path (2026-07-17). The official ggml-org llama.cpp QAT export uses general.architecture=gemma-embedding (GQA 3 heads / 1 kv, head_dim 256) and a SentencePiece tokenizer (tokenizer.ggml.model=llama, scores, NO merges). Two bugs it caught: (1) it CRASHED on load — the hyphenated arch missed the decoder allow-list and fell through to the MHA encoder graph, whose QKV reshape overran the GQA K/V (GGML_ASSERT); (2) once routed, the decoder tokenizer loader hardcoded BPE and char-tokenized every input (0 merges) -> garbage embeddings (margin 0.038). Fix = arch-gated routing to decoder_embed + an SPM-BPE bigram-merge tokenizer mode. This file also has the SentenceTransformers Dense head baked in (dense.0/1.weight, via models/add-st-dense-to-gguf.py); with it cos(crispembed, HF full SentenceTransformer) = 0.984 vs vanilla google/embeddinggemma-300m (this derives from the QAT checkpoint, whose weights differ slightly from vanilla — not a defect). Guards routing + SPM-BPE tokenizer + Dense together.",
      "hf_repo": "google/embeddinggemma-300m",
      "hf_trust_remote_code": false,
      "min_hf_cos": 0.95,
      "measured_hf_cos": 0.9852,
      "measured_note": "tests/hf_parity_community.py vs the REAL sentence-transformers pipeline (transformer + mean-pool + 2_Dense/3_Dense + L2): min=0.9852 mean=0.9891 (per-text 0.9920/0.9902/0.9852). Sits below the ~0.999 of the encoder entries for two reasons, NEITHER a bug: (1) this GGUF derives from the ggml-org QAT checkpoint, whose weights differ slightly from the vanilla google/embeddinggemma-300m used as the reference here; (2) the known gemma3-backbone discrepancy amplified by the Dense bottleneck (LEARNINGS -> EmbeddingGemma), plus q8_0. Backbone control isolating (2): cos(crispembed, HF PRE-Dense mean-pool) = 0.9835, i.e. the gap is already present before Dense and is not a Dense/pooling error. min_hf_cos=0.95 sits under that floor while still catching a structural break (wrong tokenizer/rope/pooling craters it far below — the pre-fix char-tokenizing loader gave margin 0.038). No control_file: prove_quant_control.py's per-stage dumper is encoder-only (same as the Qwen3/LFM2 decoder entries). NOTE the local run warns the model was built with sentence-transformers 5.1.0 while this env has 4.1.0."
    }
  ]
}